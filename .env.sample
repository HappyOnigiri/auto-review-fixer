# Optional: Set GitHub token if not using gh CLI authentication
# GH_TOKEN=your_github_token_here

# Optional: Set Claude token if not using claude CLI authentication
# CLAUDE_CODE_OAUTH_TOKEN=your_claude_token_here
