name: Run Refix (Single PR)

on:
  check_suite:
    types: [completed]
  pull_request:
    types: [opened, synchronize, reopened, labeled]
  workflow_dispatch:
    inputs:
      pr-number:
        description: 'PR number to process'
        required: true
        type: number

concurrency:
  group: run-refix-pr-${{ github.event.pull_request.number || github.event.check_suite.pull_requests[0].number || inputs.pr-number || 'dispatch' }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  run-refix:
    if: github.event_name != 'check_suite' || github.event.check_suite.pull_requests[0].number != 0
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Resolve PR number
        id: resolve-pr
        run: |
          PR_NUMBER="${{ github.event.pull_request.number || github.event.check_suite.pull_requests[0].number || inputs.pr-number }}"
          if [ -z "$PR_NUMBER" ]; then
            echo "Error: Could not determine PR number from event context or inputs" >&2
            exit 1
          fi
          echo "pr-number=$PR_NUMBER" >> "$GITHUB_OUTPUT"

      - name: Generate config
        env:
          REFIX_CONFIG_YAML: ${{ vars.REFIX_CONFIG_YAML }}
        run: |
          if [ -z "$REFIX_CONFIG_YAML" ]; then
            echo "Error: REFIX_CONFIG_YAML is not set or empty" >&2
            exit 1
          fi
          printf '%s\n' "$REFIX_CONFIG_YAML" > .refix.yaml

      - uses: ./
        with:
          pr-number: ${{ steps.resolve-pr.outputs.pr-number }}
          gh-token: ${{ secrets.GH_TOKEN }}
          claude-code-oauth-token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          config-path: ${{ github.workspace }}/.refix.yaml
